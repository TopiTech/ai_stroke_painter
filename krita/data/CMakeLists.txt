# Runtime data is intentionally limited to native document compatibility and
# colour management.  General painting workspaces, templates, and brush
# resource bundles are deliberately not shipped with this AI-first app.
add_subdirectory( profiles )
add_subdirectory( shortcuts )
add_subdirectory( metadata/schemas )
